# ✅ 學生功能實施檢查清單

## Phase 1: 準備工作 (15 分鐘)

### 數據庫驗證
- [ ] 連接 MySQL: `mysql -h localhost -P 3305 -u root -p626Angel`
- [ ] 進入數據庫: `USE ai_ta_db;`
- [ ] 驗證 4 個新表存在:
  ```sql
  SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES 
  WHERE TABLE_SCHEMA = 'ai_ta_db' 
  AND TABLE_NAME IN ('similar_questions', 'mistake_reasons', 'teaching_sessions', 'teaching_suggestions');
  ```
- [ ] 應顯示 4 行結果

### 後端驗證
- [ ] 打開 `/backend/server.js`
- [ ] 搜索 "學生功能 API" - 應找到新增的 8 個端點
- [ ] 驗證 API 端點:
  - [ ] GET /api/questions
  - [ ] GET /api/questions/:id/similar
  - [ ] POST /api/student/mistake-reasons
  - [ ] GET /api/student/mistake-reasons/:questionId
  - [ ] POST /api/teaching-sessions
  - [ ] GET /api/teaching-sessions/:id
  - [ ] GET /api/student/mistakes
  - [ ] GET /api/student/stats

### 前端組件驗證
- [ ] 檢查 `/frontend/src/views/student/QuestionSearch.vue` 存在
- [ ] 檢查 `/frontend/src/views/student/MistakeClinicEnhanced.vue` 存在
- [ ] 檢查 `/frontend/src/components/teaching/WhiteboardCanvas.vue` 存在

---

## Phase 2: 後端啟動 (5 分鐘)

### 啟動後端服務
```bash
cd /backend
npm start
```

- [ ] 看到消息: "Server is running on port 3001"
- [ ] 無錯誤信息

### 驗證連接
```bash
curl http://localhost:3001/api/units
```

- [ ] 返回 200 狀態碼
- [ ] 返回 JSON 格式的單元列表

---

## Phase 3: 前端路由配置 (10 分鐘)

### 編輯路由文件
**文件**: `/frontend/src/router/index.js`

- [ ] 打開路由配置文件
- [ ] 在頂部導入區添加:
  ```javascript
  import QuestionSearch from '@/views/student/QuestionSearch.vue';
  import MistakeClinicEnhanced from '@/views/student/MistakeClinicEnhanced.vue';
  ```

- [ ] 在 `student` 路由組下添加:
  ```javascript
  {
    path: 'search',
    name: 'question-search',
    component: QuestionSearch
  },
  {
    path: 'mistakes',
    name: 'mistake-clinic',
    component: MistakeClinicEnhanced
  }
  ```

- [ ] 保存文件
- [ ] 檢查無語法錯誤

### 驗證路由
- [ ] 確認 `router/index.js` 正確編輯
- [ ] 沒有重複的路由定義
- [ ] 所有導入路徑正確

---

## Phase 4: 導航菜單更新 (10 分鐘)

### 編輯學生布局文件
**文件**: `/frontend/src/views/student/StudentLayout.vue`

- [ ] 打開文件
- [ ] 找到導航區域
- [ ] 添加新的導航鏈接:
  ```vue
  <router-link to="/student/search" class="nav-link">
    🔍 搜題
  </router-link>
  <router-link to="/student/mistakes" class="nav-link">
    🏥 錯題診所
  </router-link>
  ```

- [ ] 保存文件

### 驗證導航
- [ ] 檢查導航鏈接路徑正確
- [ ] 沒有拼寫錯誤

---

## Phase 5: 前端啟動 (5 分鐘)

### 啟動前端開發服務器
```bash
cd /frontend
npm run dev
```

- [ ] 看到消息: "VITE v... ready in ... ms"
- [ ] 看到本地訪問地址 (通常 http://localhost:5173)
- [ ] 無錯誤信息
- [ ] 無警告信息（如有，記錄以便後續處理）

---

## Phase 6: 功能測試 (30 分鐘)

### Test Case 1: 訪問搜題頁面
- [ ] 打開瀏覽器到前端地址
- [ ] 登錄學生賬號
- [ ] 點擊導航中的 "🔍 搜題"
- [ ] 應進入 QuestionSearch 頁面
- [ ] 頁面標題顯示 "🔍 搜題 - 學生講題"
- [ ] 左側顯示搜索控制區
- [ ] 右側顯示空白區域（等待題目選擇）

### Test Case 2: 科目和單元選擇
- [ ] 科目下拉菜單默認為 "數學"
- [ ] 點擊 "選擇單元"
- [ ] 打開瀏覽器開發者工具 (F12)
- [ ] 檢查 Network 標籤
- [ ] 應看到對 `/api/units` 的 GET 請求
- [ ] 響應應為 200，返回 JSON 單元列表
- [ ] 單元下拉菜單應填充數據

### Test Case 3: 題目搜尋
- [ ] 選擇一個單元
- [ ] 點擊 "搜尋題目" 按鈕
- [ ] 在 Network 標籤中應看到 `/api/questions` 請求
- [ ] 響應 200，返回題目列表
- [ ] 左側應顯示題目列表

### Test Case 4: 題目選擇和預覽
- [ ] 點擊列表中的一題
- [ ] 右側應顯示題目詳情
- [ ] 顯示題目文本、答案、解答

### Test Case 5: 相似題推薦
- [ ] 點擊 "AI 類似題" 或相似題按鈕
- [ ] 在 Network 中應看到 `/api/questions/:id/similar` 請求
- [ ] 應顯示 3 道相似題目
- [ ] 每道題都能點擊查看

### Test Case 6: 白板功能
- [ ] 右側下方應顯示白板畫布
- [ ] 在白板上用滑鼠繪畫應該可以工作
- [ ] 有 "清空" 按鈕清除繪畫

### Test Case 7: 錯題原因選擇
- [ ] 點擊 "完成講題"
- [ ] 應彈出錯題原因對話框
- [ ] 應顯示 6 個原因選項
- [ ] 選擇一個原因
- [ ] 在 Network 中應看到 POST 到 `/api/student/mistake-reasons`
- [ ] 響應應為 200，顯示 "success": true

### Test Case 8: 訪問錯題診所
- [ ] 點擊導航中的 "🏥 錯題診所"
- [ ] 應進入 MistakeClinic 頁面
- [ ] 頁面標題顯示 "🏥 錯題診所"
- [ ] 應顯示統計卡片（總錯題、本週、最常原因）

### Test Case 9: 錯題列表和診斷
- [ ] 應顯示錯題列表
- [ ] 每個錯題卡片顯示題目信息
- [ ] 對於已診斷的錯題，顯示 "診斷結果"
- [ ] 對於未診斷的錯題，顯示 6 個原因選項
- [ ] 點擊一個原因
- [ ] 應在 Network 中看到 POST 請求
- [ ] 原因應被保存並顯示在卡片上

### Test Case 10: 改進建議
- [ ] 點擊 "💡 獲取改進建議"
- [ ] 應彈出建議模態框
- [ ] 應顯示 3-5 條針對性建議
- [ ] 建議應與選擇的原因相關

### Test Case 11: 自訂原因
- [ ] 對於未診斷的錯題，點擊 "✏️ 自訂原因"
- [ ] 應顯示文本區輸入框
- [ ] 輸入一個自訂原因
- [ ] 點擊 "保存原因"
- [ ] 應看到 POST 請求，reason_type = "other"
- [ ] 原因應被保存

### Test Case 12: 統計和分布
- [ ] 進入錯題診所
- [ ] 檢查統計卡片是否正確:
  - [ ] 總錯題數正確
  - [ ] 本週新增數正確
  - [ ] 最常原因正確
- [ ] 在 Network 中應看到 `/api/student/stats` 請求

---

## Phase 7: API 集成測試 (20 分鐘)

### API 1: 搜尋題目
```bash
curl -X GET "http://localhost:3001/api/questions?unit_id=1&difficulty=easy" \
  -H "user-id: 1"
```

- [ ] 返回 200 狀態碼
- [ ] 返回 JSON 格式:
  ```json
  {
    "questions": [...]
  }
  ```
- [ ] questions 數組不為空
- [ ] 每個題目包含: id, unit_id, question_text, answer_text, difficulty

### API 2: 獲取相似題
```bash
curl -X GET "http://localhost:3001/api/questions/1/similar" \
  -H "user-id: 1"
```

- [ ] 返回 200 狀態碼
- [ ] 返回 JSON 格式:
  ```json
  {
    "similarQuestions": [...]
  }
  ```

### API 3: 保存錯題原因
```bash
curl -X POST "http://localhost:3001/api/student/mistake-reasons" \
  -H "user-id: 1" \
  -H "Content-Type: application/json" \
  -d '{"question_id": 1, "reason_type": "calculation"}'
```

- [ ] 返回 200 狀態碼
- [ ] 返回 JSON:
  ```json
  {
    "success": true,
    "id": <number>,
    "message": "錯題原因已保存"
  }
  ```

### API 4: 獲取所有錯題
```bash
curl -X GET "http://localhost:3001/api/student/mistakes" \
  -H "user-id: 1"
```

- [ ] 返回 200 狀態碼
- [ ] 返回 mistakes 數組

### API 5: 獲取統計
```bash
curl -X GET "http://localhost:3001/api/student/stats" \
  -H "user-id: 1"
```

- [ ] 返回 200 狀態碼
- [ ] 返回:
  ```json
  {
    "totalMistakes": <number>,
    "weeklyMistakes": <number>,
    "reasonDistribution": [...]
  }
  ```

---

## Phase 8: 數據驗證 (10 分鐘)

### 驗證數據庫記錄

```sql
-- 檢查錯題原因記錄
SELECT * FROM mistake_reasons WHERE student_id = 1;
```

- [ ] 應看到之前保存的記錄
- [ ] reason_type 值正確
- [ ] recorded_at 時間戳正確

```sql
-- 檢查教學會話（如果已保存）
SELECT * FROM teaching_sessions WHERE student_id = 1;
```

- [ ] 如果有會話記錄，應顯示會話信息

```sql
-- 檢查相似題關聯
SELECT * FROM similar_questions LIMIT 5;
```

- [ ] 應顯示相似題關聯記錄

```sql
-- 驗證統計數據
SELECT reason_type, COUNT(*) as count 
FROM mistake_reasons 
WHERE student_id = 1 
GROUP BY reason_type;
```

- [ ] 應與前端顯示的統計一致

---

## Phase 9: 瀏覽器兼容性測試 (10 分鐘)

- [ ] Chrome/Edge: 測試所有功能
- [ ] Firefox: 測試白板和API
- [ ] Safari (如適用): 測試基本功能
- [ ] 移動瀏覽器: 測試響應式設計
  - [ ] 搜題頁面在手機上單列顯示
  - [ ] 白板在手機上可用
  - [ ] 對話框在手機上可見

---

## Phase 10: 性能和錯誤處理 (15 分鐘)

### 性能測試
- [ ] 搜尋大量題目時性能正常
- [ ] 加載錯題列表時無明顯延遲
- [ ] API 響應時間 < 500ms

### 錯誤處理測試
- [ ] 斷開網絡 → 應顯示錯誤提示
- [ ] 輸入無效 unit_id → 返回空列表或錯誤
- [ ] 未登錄訪問搜題頁 → 重定向或提示
- [ ] 選擇任何原因都能正常保存
- [ ] 重複選擇原因應能覆蓋舊值

### 控制台檯錯誤
- [ ] 打開開發者工具 Console 標籤
- [ ] 應無紅色錯誤信息
- [ ] 警告信息可接受（Vue 開發模式警告等）

---

## Phase 11: 文檔完整性 (5 分鐘)

- [ ] `/docs/STUDENT_FEATURES_COMPLETE.md` 存在
- [ ] `/docs/QUICK_START_STUDENT.md` 存在
- [ ] 文檔內容清晰、完整
- [ ] 代碼示例正確

---

## Phase 12: 提交和部署檢查 (5 分鐘)

### Git 提交準備
- [ ] 所有修改文件已保存
- [ ] 無未跟踪的文件需要 .gitignore
- [ ] 代碼風格一致
- [ ] 所有功能測試通過

### 部署檢查清單
- [ ] 數據庫遷移命令記錄
- [ ] 環境變量配置正確
- [ ] 後端伺服器啟動命令文檔
- [ ] 前端構建命令文檔

---

## 問題排查

### 問題 1: 路由無法訪問
**症狀**: 點擊搜題鏈接無反應或 404

**排查**:
- [ ] 檢查 router/index.js 是否正確編輯
- [ ] 確認組件路徑拼寫正確
- [ ] 重新啟動前端開發服務器
- [ ] 清除瀏覽器緩存 (Ctrl+Shift+Delete)

### 問題 2: API 返回 404
**症狀**: Network 中看到 404 錯誤

**排查**:
- [ ] 檢查後端服務是否運行
- [ ] 檢查 API 端點是否正確拼寫
- [ ] 檢查 user-id 請求頭是否包含
- [ ] 查看後端日誌是否有錯誤

### 問題 3: 數據不顯示
**症狀**: 搜尋後題目列表為空

**排查**:
- [ ] 檢查 questions 表是否有數據
- [ ] 檢查 unit_id 是否正確
- [ ] 檢查 difficulty 篩選是否過於嚴格
- [ ] 使用 SQL 直接查詢驗證數據

### 問題 4: 白板無法繪畫
**症狀**: 在白板上繪畫無反應

**排查**:
- [ ] 檢查 WhiteboardCanvas.vue 是否正確導入
- [ ] 檢查瀏覽器是否支持 HTML5 Canvas
- [ ] 檢查 Canvas 元素是否有正確的寬高
- [ ] 檢查滑鼠事件監聽是否正確綁定

### 問題 5: 原因保存失敗
**症狀**: 選擇原因後無反應

**排查**:
- [ ] 檢查 Network 中 POST 請求狀態
- [ ] 檢查後端是否有錯誤日誌
- [ ] 檢查 mistake_reasons 表是否存在
- [ ] 檢查 reason_type 值是否有效

---

## 成功標準

✅ **所有測試通過時**:

1. ✅ 8 個 API 端點均返回 200 狀態碼
2. ✅ 搜尋功能正常工作
3. ✅ 相似題推薦正常工作
4. ✅ 錯題原因可成功保存
5. ✅ 錯題診所可顯示所有錯題
6. ✅ 統計信息正確計算
7. ✅ 改進建議正確生成
8. ✅ 白板可正常繪畫
9. ✅ 數據庫記錄正確保存
10. ✅ 無控制台錯誤信息

---

## 簽出表

| 檢查項目 | 狀態 | 簽名 | 日期 |
|---------|------|------|------|
| 數據庫驗證 | ☐ 完成 | _____ | _______ |
| 後端驗證 | ☐ 完成 | _____ | _______ |
| 路由配置 | ☐ 完成 | _____ | _______ |
| 導航更新 | ☐ 完成 | _____ | _______ |
| 功能測試 | ☐ 完成 | _____ | _______ |
| API 測試 | ☐ 完成 | _____ | _______ |
| 數據驗證 | ☐ 完成 | _____ | _______ |
| 兼容性測試 | ☐ 完成 | _____ | _______ |
| 錯誤處理 | ☐ 完成 | _____ | _______ |
| **最終驗收** | **☐ 完成** | **_____** | **_______** |

---

**估計時間**: 2.5-3 小時  
**難度等級**: ⭐⭐☆☆☆  
**風險等級**: 低  
**狀態**: 🟢 準備部署
